dist: xenial

language: python

python:
  - "3.6"
  - "3.7"

install:
  - "pip install coverage flake8 pint pytest pytest-cov python-coveralls"
  - "pip install -r requirements.txt"

script:
  - "python setup.py install"
  - "py.test tests/ --cov ate --cov-report term-missing"
  - "flake8 ate"

after_success:
  - "coveralls"
